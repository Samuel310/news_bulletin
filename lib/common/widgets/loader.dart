import 'package:flutter/material.dart';
import 'package:flutter_spinkit/flutter_spinkit.dart';

const initialLoader = SpinKitPulse(
  color: Colors.blue,
  size: 50.0,
);

const newsLoader = SpinKitWave(
  color: Colors.blue,
  size: 20,
);
